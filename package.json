{
  "name": "rspress-doc-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf doc_build",
    "dev": "rspress dev",
    "build": "pnpm clean && rspress build",
    "preview": "rspress preview"
  },
  "dependencies": {
    "rspress": "^1.26.1"
  },
  "devDependencies": {
    "@types/node": "^16"
  }
}
